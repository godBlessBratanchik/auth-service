# 🧪 Полное руководство по тестированию CI/CD

## 📌 Важно понимать

**CI/CD pipeline** публикует Docker образы в DockerHub. Но сначала нужно запустить pipeline, чтобы образ появился!

---

## 🎯 Правильная последовательность тестирования

### ЭТАП 1: Локальное тестирование и сборка
### ЭТАП 2: Push в GitHub и запуск CI/CD
### ЭТАП 3: Проверка образа на DockerHub
### ЭТАП 4: Использование опубликованного образа

---

## 🚀 ЭТАП 1: Локальное тестирование

### Шаг 1.1: Соберите образ локально

```bash
# Сборка auth-service образа
docker-compose build auth-service
```

**Ожидаемое время:** 3-5 минут (первый раз)

**Что происходит:**
- Maven скачивает зависимости
- Компилируется Java код
- Собирается JAR файл
- Создается Docker образ

### Шаг 1.2: Запустите все сервисы

```bash
# Запуск всех сервисов
docker-compose up -d

# Или используйте скрипт (он автоматически сделает build)
start-local.bat
```

### Шаг 1.3: Проверьте статус

```bash
docker-compose ps
```

**Ожидаемый результат:**
```
NAME                STATUS
auth-postgres       Up (healthy)
auth-service        Up
auth-kafka          Up
auth-zookeeper      Up
```

### Шаг 1.4: Проверьте логи

```bash
# Следите за логами auth-service
docker-compose logs -f auth-service
```

**Ищите:**
- ✅ `Started AuthServiceApplication in X seconds`
- ✅ `Tomcat started on port 8080`
- ❌ НЕТ ошибок типа "Connection refused"

### Шаг 1.5: Проверьте подключение к БД

```bash
# Подключитесь к PostgreSQL
docker exec -it auth-postgres psql -U postgres -d authdb

# Посмотрите таблицы
\dt

# Выход
\q
```

### Шаг 1.6: Тестируйте API

```bash
# Проверьте что сервис отвечает
curl http://localhost:8080

# PowerShell:
Invoke-WebRequest http://localhost:8080

# Если есть endpoints для регистрации (пример):
curl -X POST http://localhost:8080/api/auth/register ^
  -H "Content-Type: application/json" ^
  -d "{\"username\":\"test\",\"email\":\"test@test.com\",\"password\":\"pass123\"}"
```

✅ **Если все работает локально - переходите к Этапу 2**

---

## 📤 ЭТАП 2: Push в GitHub и запуск CI/CD

### Шаг 2.1: Проверьте что изменилось

```bash
git status
```

Вы должны увидеть новые/измененные файлы:
- `.github/workflows/ci.yml` (изменен)
- `.dockerignore` (новый)
- `docker-compose.yml` (изменен)
- Документация (новые .md файлы)

### Шаг 2.2: Добавьте все изменения

```bash
# Добавить все файлы
git add .

# Проверить что будет закоммичено
git status
```

### Шаг 2.3: Сделайте коммит

```bash
git commit -m "feat: configure CI/CD with DockerHub integration

- Updated GitHub Actions workflow with DockerHub push
- Added docker-compose.yml for local development
- Added comprehensive documentation
- Added troubleshooting guides
- Configured database connection handling"
```

### Шаг 2.4: Отправьте на GitHub

```bash
# Убедитесь что вы на правильной ветке
git branch

# Push в GitHub
git push origin main
```

**Примечание:** Если ваша основная ветка называется `master` или `develop`, используйте соответствующее имя.

### Шаг 2.5: Откройте GitHub Actions

1. Откройте ваш репозиторий на GitHub
2. Перейдите во вкладку **Actions**
3. Вы должны увидеть запущенный workflow "Auth Service CI/CD"

### Шаг 2.6: Наблюдайте за выполнением

**Job 1: Run Tests** ⏱️ ~1-2 минуты
```
✅ Checkout code
✅ Set up JDK 17
✅ Run tests (mvn clean test)
✅ Upload test results
```

**Job 2: Build and Push to DockerHub** ⏱️ ~3-5 минут
```
✅ Checkout code
✅ Set up Docker Buildx
✅ Log in to DockerHub
✅ Extract metadata for Docker
✅ Build and push Docker image
✅ Image digest: sha256:...
```

### Шаг 2.7: Проверьте результаты

**Успешное выполнение:**
- ✅ Зеленая галочка возле workflow run
- ✅ Оба job'а зеленые
- ✅ В логах последнего шага видно: "Image pushed with digest sha256:..."

**Если есть ошибки:**

#### Ошибка в "Run Tests"
```bash
# Запустите тесты локально чтобы увидеть проблему
mvn clean test
```

#### Ошибка в "Log in to DockerHub"
- Проверьте секреты в GitHub: Settings → Secrets and variables → Actions
- Должны быть: `DOCKER_USERNAME` и `DOCKER_TOKEN`
- См. `GITHUB-SECRETS-SETUP.md`

#### Ошибка в "Build and push"
- Проверьте логи Docker build в GitHub Actions
- Возможно проблема с Dockerfile или зависимостями Maven

---

## 🐋 ЭТАП 3: Проверка образа на DockerHub

### Шаг 3.1: Откройте DockerHub

Перейдите на: https://hub.docker.com/r/elbondarenko04121/auth-service

**Или:**
1. Откройте https://hub.docker.com
2. Войдите под `elbondarenko04121`
3. Найдите репозиторий `auth-service`

### Шаг 3.2: Проверьте теги

Вы должны увидеть:
- `latest` (если push был в main/master)
- `main` или название вашей ветки
- `main-abc1234` (ветка + короткий SHA)

### Шаг 3.3: Проверьте детали

- 📅 **Last pushed**: текущая дата/время
- 📦 **Compressed Size**: ~100-150 MB
- 🖥️ **OS/Arch**: linux/amd64
- 📝 **Digest**: sha256:...

### Шаг 3.4: Скачайте образ

```bash
# Скачайте образ с DockerHub
docker pull elbondarenko04121/auth-service:latest

# Проверьте что образ скачался
docker images | findstr auth-service
```

---

## ✅ ЭТАП 4: Использование опубликованного образа

### Шаг 4.1: Обновите docker-compose.yml (опционально)

Если хотите всегда использовать образ с DockerHub (а не локальную сборку):

```yaml
auth-service:
  image: elbondarenko04121/auth-service:latest
  # Уберите секцию build:
```

### Шаг 4.2: Остановите текущие контейнеры

```bash
docker-compose down
```

### Шаг 4.3: Запустите с образом из DockerHub

```bash
# Docker Compose автоматически скачает latest образ
docker-compose up -d

# Или явно скачайте и запустите
docker-compose pull
docker-compose up -d
```

### Шаг 4.4: Проверьте что используется образ с DockerHub

```bash
# Посмотрите откуда образ
docker images | findstr auth-service

# В выводе должно быть:
# elbondarenko04121/auth-service   latest   [IMAGE_ID]   [SIZE]
```

---

## 🔄 Цикл разработки (после первоначальной настройки)

После того как CI/CD настроен, цикл выглядит так:

```
1. Разработка локально
   ├─ Изменяете код
   ├─ Тестируете: docker-compose up -d
   └─ Проверяете что работает

2. Коммит и push
   ├─ git add .
   ├─ git commit -m "..."
   └─ git push origin main

3. GitHub Actions (автоматически)
   ├─ Запускает тесты
   ├─ Собирает Docker образ
   └─ Публикует в DockerHub

4. Deployment (в любом окружении)
   └─ docker pull elbondarenko04121/auth-service:latest
```

**Время полного цикла:** ~5-7 минут от push до готового образа

---

## 📊 Чеклист полного тестирования

### ✅ Локально (Этап 1)
- [ ] `docker-compose build auth-service` - успешно
- [ ] `docker-compose up -d` - все контейнеры Up
- [ ] `docker-compose ps` - все healthy/running
- [ ] `docker-compose logs auth-service` - нет ошибок JDBC
- [ ] `docker exec -it auth-postgres psql -U postgres -d authdb -c "\dt"` - видны таблицы
- [ ] `curl http://localhost:8080` - сервис отвечает

### ✅ GitHub Actions (Этап 2)
- [ ] Секреты `DOCKER_USERNAME` и `DOCKER_TOKEN` настроены
- [ ] `git push origin main` - выполнен
- [ ] Workflow "Auth Service CI/CD" запустился
- [ ] Job "Run Tests" - зеленый ✅
- [ ] Job "Build and Push to DockerHub" - зеленый ✅
- [ ] В логах видно "Image pushed with digest"

### ✅ DockerHub (Этап 3)
- [ ] Репозиторий `elbondarenko04121/auth-service` существует
- [ ] Видны теги: `latest`, `main`, `main-{sha}`
- [ ] Last pushed - сегодняшняя дата
- [ ] `docker pull elbondarenko04121/auth-service:latest` работает

### ✅ Production-ready (Этап 4)
- [ ] Образ с DockerHub запускается без ошибок
- [ ] Можно развернуть командой: `docker-compose up -d`
- [ ] Подключение к БД работает
- [ ] API endpoints отвечают

---

## 🎯 Краткая версия (TL;DR)

```bash
# 1. Локальное тестирование
docker-compose build auth-service
docker-compose up -d
docker-compose logs -f auth-service
# Убедитесь что работает!

# 2. Push в GitHub
git add .
git commit -m "feat: configure CI/CD with DockerHub"
git push origin main

# 3. Откройте GitHub Actions
# https://github.com/ваш-username/auth-service/actions
# Дождитесь зеленых галочек (~5 минут)

# 4. Проверьте DockerHub
# https://hub.docker.com/r/elbondarenko04121/auth-service
# Образ должен появиться!

# 5. Используйте образ
docker pull elbondarenko04121/auth-service:latest
docker-compose up -d
```

---

## 🐛 Troubleshooting

### "Error: pull access denied" при docker-compose up

**Причина:** Образ еще не опубликован на DockerHub

**Решение:**
```bash
# Вариант 1: Соберите локально
docker-compose build auth-service
docker-compose up -d

# Вариант 2: Дождитесь пока CI/CD опубликует образ
# Сделайте push в GitHub и подождите 5 минут
```

### GitHub Actions падает на тестах

**Решение:**
```bash
# Запустите тесты локально
mvn clean test

# Посмотрите какой тест падает
mvn test -X
```

### GitHub Actions не может залогиниться в DockerHub

**Решение:**
1. Settings → Secrets and variables → Actions
2. Проверьте наличие секретов
3. См. `GITHUB-SECRETS-SETUP.md`

### Образ не появляется на DockerHub

**Проверьте:**
- Job "Build and Push" завершился успешно?
- В логах есть строка "Image pushed"?
- Правильные credentials в секретах?

---

## 📞 Дополнительная помощь

Если что-то не работает, см.:
- `TROUBLESHOOTING-DATABASE.md` - проблемы с БД
- `GITHUB-SECRETS-SETUP.md` - настройка секретов
- `CI-CD-SETUP.md` - детали CI/CD
- `QUICKSTART.md` - быстрый старт

---

**Начинайте с Этапа 1 и следуйте последовательно!** 🚀

*Обновлено: 17 декабря 2025*


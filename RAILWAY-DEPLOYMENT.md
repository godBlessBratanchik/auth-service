# 🚂 Railway Deployment Guide

## 📋 Пошаговая инструкция по деплою Auth Service на Railway

---

## ✅ ЧТО ВАМ НУЖНО СДЕЛАТЬ

### Шаг 1: Создать проект на Railway (~2 минуты)

1. **Откройте:** https://railway.app
2. **Нажмите:** "Login" → войти через **GitHub**
3. **Нажмите:** "New Project"
4. **Выберите:** "Deploy from GitHub repo"
5. **Выберите:** ваш репозиторий `godBlessBratanchik/auth-service`
6. Railway автоматически обнаружит `Dockerfile` и начнёт деплой

**Ожидаемый результат:** Проект создан, идёт первая сборка

---

### Шаг 2: Добавить PostgreSQL (~1 минута)

1. В вашем проекте нажмите **"+ New"**
2. Выберите **"Database"** → **"Add PostgreSQL"**
3. Railway создаст базу данных автоматически

**Ожидаемый результат:** 
- PostgreSQL контейнер создан
- Переменные `DATABASE_URL`, `PGHOST`, `PGPORT`, `PGUSER`, `PGPASSWORD`, `PGDATABASE` доступны

---

### Шаг 3: Настроить переменные окружения (~3 минуты)

1. **Кликните** на ваш сервис `auth-service` (НЕ на базу данных!)
2. Перейдите во вкладку **"Variables"**
3. Нажмите **"+ New Variable"** и добавьте каждую переменную:

#### Обязательные переменные:

```bash
# Database (Railway подставит автоматически)
SPRING_DATASOURCE_URL=${DATABASE_URL}

# Database Driver
SPRING_DATASOURCE_DRIVER_CLASS_NAME=org.postgresql.Driver

# JPA/Hibernate
SPRING_JPA_HIBERNATE_DDL_AUTO=update
SPRING_JPA_DATABASE_PLATFORM=org.hibernate.dialect.PostgreSQLDialect

# Profile
SPRING_PROFILES_ACTIVE=prod

# Server
SERVER_PORT=8080

# JWT (ВАЖНО: смените на свой секретный ключ!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Kafka (отключаем для Railway)
SPRING_KAFKA_ENABLED=false
```

**Примечание:** 
- `${DATABASE_URL}` - Railway автоматически подставит URL PostgreSQL
- Для `JWT_SECRET` используйте сложный случайный ключ!

---

### Шаг 4: Настроить деплой и получить URL (~2 минуты)

1. Кликните на ваш сервис `auth-service`
2. Перейдите в **"Settings"**
3. В разделе **"Environment"** убедитесь что выбрана ветка `main`
4. Включите **"Deploy Trigger"** (должен быть включен по умолчанию)
5. В разделе **"Networking"**:
   - Нажмите **"Generate Domain"**
   - Скопируйте полученный URL (например: `auth-service-production.up.railway.app`)

**Ожидаемый результат:** Публичный URL для вашего сервиса

---

### Шаг 5: Создать Railway API токен (~2 минуты)

1. Кликните на **ваш профиль** (иконка в правом верхнем углу)
2. Выберите **"Account Settings"**
3. Перейдите в **"Tokens"**
4. Нажмите **"Create Token"**
   - **Token Name:** `github-actions`
   - Нажмите **"Create"**
5. **⚠️ СКОПИРУЙТЕ ТОКЕН** (показывается только один раз!)

**Ожидаемый результат:** Токен скопирован в буфер обмена

---

### Шаг 6: Добавить токен в GitHub Secrets (~1 минута)

1. **Откройте:** https://github.com/godBlessBratanchik/auth-service/settings/secrets/actions
2. Нажмите **"New repository secret"**
3. Заполните:
   - **Name:** `RAILWAY_TOKEN`
   - **Secret:** вставьте скопированный токен Railway
4. Нажмите **"Add secret"**

**Ожидаемый результат:** Секрет `RAILWAY_TOKEN` добавлен в GitHub

---

### Шаг 7: Запустить деплой через GitHub Actions

После того как вы:
- ✅ Настроили проект на Railway
- ✅ Добавили PostgreSQL
- ✅ Настроили переменные окружения
- ✅ Добавили `RAILWAY_TOKEN` в GitHub Secrets

**Скажите мне, и я сделаю push!** Workflow автоматически задеплоит на Railway.

---

## 🔍 Проверка развертывания

### После успешного деплоя:

1. **Проверьте логи в Railway:**
   - Откройте ваш проект на Railway
   - Кликните на сервис `auth-service`
   - Перейдите во вкладку **"Deployments"**
   - Кликните на последний deployment
   - Проверьте логи - должны видеть `Started AuthServiceApplication`

2. **Проверьте health endpoint:**
   ```bash
   curl https://ваш-домен.up.railway.app/actuator/health
   ```
   Должны получить: `{"status":"UP"}`

3. **Проверьте базу данных:**
   - В Railway кликните на PostgreSQL
   - Перейдите в **"Data"**
   - Должна появиться таблица `users` (создана Hibernate)

---

## 📊 Архитектура на Railway

```
┌─────────────────────────────────────────┐
│           Railway Project                │
│                                          │
│  ┌────────────────┐  ┌────────────────┐│
│  │  auth-service  │  │  PostgreSQL 15 ││
│  │                │─▶│                ││
│  │  Spring Boot   │  │   Database     ││
│  │  Port: 8080    │  │   Port: 5432   ││
│  └────────────────┘  └────────────────┘│
│         │                                │
│    Public URL                            │
└─────────┼────────────────────────────────┘
          │
          ▼
   https://auth-service-production.up.railway.app
```

---

## 🔄 Процесс автоматического деплоя

```
1. Вы: git push origin main
         ↓
2. GitHub Actions:
   ✅ Checkout кода
   ✅ Установка Railway CLI
   ✅ Deploy: railway up
         ↓
3. Railway:
   ✅ Обнаружение Dockerfile
   ✅ Сборка Docker образа
   ✅ Push образа в Railway Registry
   ✅ Развертывание контейнера
   ✅ Подключение к PostgreSQL
   ✅ Запуск на публичном URL
         ↓
4. Готово! Сервис доступен
```

---

## 🎯 Созданные файлы

### 1. `.github/workflows/deploy-railway.yml`
- GitHub Actions workflow для автоматического деплоя
- Использует Railway CLI
- Триггерится при push в main

### 2. `src/main/resources/application-prod.properties`
- Production конфигурация
- Использует переменные окружения Railway
- Оптимизированные настройки для production

### 3. `railway.json`
- Конфигурация Railway
- Указывает использовать Dockerfile
- Настройки рестарта и команды запуска

---

## ⚙️ Переменные окружения Railway

| Переменная | Откуда | Описание |
|------------|--------|----------|
| `DATABASE_URL` | Railway PostgreSQL | Полный JDBC URL базы данных |
| `PGHOST` | Railway PostgreSQL | Хост PostgreSQL |
| `PGPORT` | Railway PostgreSQL | Порт PostgreSQL (5432) |
| `PGUSER` | Railway PostgreSQL | Имя пользователя БД |
| `PGPASSWORD` | Railway PostgreSQL | Пароль БД |
| `PGDATABASE` | Railway PostgreSQL | Имя базы данных |
| `PORT` | Railway | Порт для приложения (авто) |

**Вы добавляете вручную:**
- `SPRING_DATASOURCE_URL`
- `SPRING_PROFILES_ACTIVE`
- `JWT_SECRET`
- и другие...

---

## 🐛 Troubleshooting

### Ошибка: "Failed to connect to database"

**Проблема:** Сервис не может подключиться к PostgreSQL

**Решение:**
1. Проверьте что PostgreSQL создан в том же проекте
2. Проверьте переменную `SPRING_DATASOURCE_URL=${DATABASE_URL}`
3. В логах Railway найдите актуальный `DATABASE_URL`

---

### Ошибка: "Port already in use"

**Проблема:** Конфликт портов

**Решение:**
1. Убедитесь что `SERVER_PORT=8080` в переменных
2. Railway автоматически настроит внешний порт

---

### Ошибка: "railway: command not found" в GitHub Actions

**Проблема:** Railway CLI не установлен

**Решение:**
- Проверьте что в workflow есть шаг установки CLI
- Уже исправлено в `deploy-railway.yml`

---

### Сервис падает при старте

**Проблема:** Ошибка в конфигурации

**Решение:**
1. Откройте Railway → Deployments → View Logs
2. Найдите строку с ошибкой
3. Проверьте переменные окружения
4. Проверьте что `SPRING_PROFILES_ACTIVE=prod`

---

## 📈 Мониторинг

### Логи Railway:
```
Railway Dashboard → Your Service → Deployments → View Logs
```

### Метрики:
```
Railway Dashboard → Your Service → Metrics
```
Показывает:
- CPU usage
- Memory usage
- Network traffic
- Request count

### Health Check:
```bash
curl https://ваш-url.up.railway.app/actuator/health
```

---

## 💰 Стоимость

**Railway Free Tier:**
- ✅ $5 кредитов в месяц (500 часов)
- ✅ Достаточно для тестов и учебы
- ✅ PostgreSQL включен

**Для production:**
- Смотрите https://railway.app/pricing
- Pay-as-you-go после free tier

---

## 🎓 Полезные ссылки

- **Railway Docs:** https://docs.railway.app
- **Railway CLI:** https://docs.railway.app/develop/cli
- **Railway Dashboard:** https://railway.app/dashboard
- **Spring Boot Railway Guide:** https://docs.railway.app/guides/spring-boot

---

## ✅ Checklist готовности к деплою

Перед push убедитесь:

- [ ] Проект создан на Railway
- [ ] PostgreSQL добавлен
- [ ] Переменные окружения настроены (все из списка)
- [ ] Domain сгенерирован
- [ ] Railway токен создан
- [ ] `RAILWAY_TOKEN` добавлен в GitHub Secrets
- [ ] Dockerfile существует (уже есть)
- [ ] railway.json создан (создан автоматически)
- [ ] application-prod.properties настроен (создан автоматически)
- [ ] deploy-railway.yml workflow создан (создан автоматически)

**Когда всё готово - скажите, и я сделаю push для деплоя!** 🚀

---

## 🎉 Результат

После успешного деплоя у вас будет:

✅ **Автоматический деплой** при каждом push  
✅ **Публичный URL** для доступа к API  
✅ **PostgreSQL база** в облаке  
✅ **Логи и метрики** в реальном времени  
✅ **Zero-downtime** обновления  

**Ваш сервис будет доступен 24/7 в интернете!** 🌐

